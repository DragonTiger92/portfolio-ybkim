name: Product backlog item
description: Track one product-facing Product Backlog Item from the planning docs.
title: "feat: "
labels:
  - type:feature
body:
  - type: markdown
    attributes:
      value: |
        Use this for one Product Backlog Item. A phase-sized pull request can close multiple issues.
  - type: input
    id: pbi_id
    attributes:
      label: Product Backlog Item ID
      description: Link this issue to `docs/planning/product-backlog.md` when possible.
      placeholder: PBI-006
    validations:
      required: false
  - type: dropdown
    id: phase
    attributes:
      label: Roadmap phase
      options:
        - PH-001
        - PH-002
        - PH-003
        - PH-004
      default: 1
    validations:
      required: true
  - type: textarea
    id: goal
    attributes:
      label: Goal
      description: What outcome should this change create?
      placeholder: State the user-visible result and why it matters.
    validations:
      required: true
  - type: textarea
    id: scope
    attributes:
      label: Scope
      description: What should be included or intentionally left out?
    validations:
      required: true
  - type: textarea
    id: acceptance
    attributes:
      label: Acceptance criteria
      description: What must be true before this issue can close?
      placeholder: "- ..."
    validations:
      required: true
  - type: dropdown
    id: release_impact
    attributes:
      label: Release impact
      options:
        - release:minor
        - release:patch
        - release:major
        - release:not-applicable
      default: 0
    validations:
      required: true
